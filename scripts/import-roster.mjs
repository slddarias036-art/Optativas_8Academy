import {SQLiteStore} from '../server/sqlite.mjs';import {initialize,importStudents} from '../server/service.mjs';import {readFileSync,mkdirSync} from 'node:fs';mkdirSync('private',{recursive:true});const s=new SQLiteStore('private/optativas.sqlite');await initialize(s);console.log(await importStudents(s,JSON.parse(readFileSync('private/roster.json','utf8')),'importacion-local'));s.close();